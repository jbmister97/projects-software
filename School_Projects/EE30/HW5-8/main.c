#include <msp430g2553.h>
/******************
 * HW5 #8
 *
 *****************************/
int x = 663,y;


void main(void)
{
	if(0 < x < 2000)
	{
		y = ((x << 3) + (x << 1) + x) + (((x << 2) + (x << 1) + x) / (x << 4));
	}
}
